{
  "name": "underscore",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:web": "cd apps/web && node_modules/.bin/vite",
    "dev:api": "pnpm --filter api start:dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
