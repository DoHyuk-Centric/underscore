# 밑줄 — 기능 명세

## 핵심 루프

```
공시 카드 / 종목 탐색
        ↓
     매수 주문
        ↓
  ★ 근거 입력 (필수, 통과 못 하면 주문 불가)
        ↓
   보유 (평가손익 갱신)
        ↓
     매도 주문
        ↓
  ★ 즉시 리뷰 (lot 전량 소진 시)
        ↓
    밑줄 기록에 누적
```

## 핵심 기능 1 — 매도 시 AI 거래 리뷰

매수 시 남긴 근거와 실제 결과를 대조해 리뷰를 생성한다.

### 3층 구조

확신은 모델에서 오지 않는다. 아키텍처에서 온다.

| 층 | 담당 | 내용 |
| --- | --- | --- |
| 계산층 (결정론적) | 서버 코드 | 손익, 벤치마크 대비 상대수익, 보유기간 고저점 대비 매도 위치, 최대낙폭, 보유기간, DART 공시 매칭 |
| 판정층 (룰) | 룰 엔진 | 근거 유형 분류(실적/모멘텀/테마·뉴스/차트/이유 없음), "검증 가능한 근거인가" 라벨 |
| 서술층 (LLM) | LLM | 위 두 층 결과만 입력받아 한국어 문장화 + 다음 매매에 적용할 질문 1개 |

- **LLM이 "아는" 지식에 절대 의존하지 않는다.** 모든 사실은 프롬프트로 주입
- 산술은 코드가 한다. LLM에게 맡기면 틀린다
- 시세는 토스증권 API, 공시는 DART가 출처

상세 검증 계획은 별도 문서(`🧪 AI 거래 리뷰 PoC 검증 계획`) 참조.

### 리뷰 서술 규칙

1. **근거와 결과가 연결되어 있는지** — 맞았다/틀렸다 판정이 아니라, 벤치마크 대비 상대수익·공시 매칭으로 "시장 덕분인가, 종목 선택 덕분인가"를 사실로 제시
2. **매도 위치와 감내한 낙폭** — 고점 대비 아쉬움을 지적 기준으로 삼지 않는다. 최고가 매도는 실전에서 불가능하므로, 기준은 **사용자가 세운 근거의 달성 여부**
3. **다음 매매에 적용할 질문 1개** — 종목을 특정한 미래 행동 지시는 금지

### 리뷰 생성 조건

- lot이 **전량 소진**될 때 1건 생성 (부분 소진 lot은 리뷰 없음)
- lot 보유기간 **1일 미만이면 리뷰 미생성** 또는 "평가할 근거가 없습니다"만 출력
- AI 호출은 이 시점 1회. 실시간 시세 스트림과 분리되어 비용·성능 관리 용이

### 단타 억제 원리

화면이나 UI를 제한해서 막지 않는다. **짧게 들고 있으면 아무 피드백도 없다**는 규칙 하나로 자연스럽게 억제한다. 억지 감점이 아니라 정직한 출력이다.

## 핵심 기능 2 — 실시간 평가손익 (리텐션 훅)

- 장중에는 실제 시세로 평가손익이 갱신된다
- 여러 날에 걸친 보유는 일봉(종가) 기준으로 이어진다. 장이 닫히므로 자연스러운 것이지 인위적 제약이 아니다
- 상승/하락 색상 전환 + 숫자 변경 플래시로 체감 재미

### 확정 사항

- **UI는 진짜 증권 앱.** 턴제·"다음 날" 버튼 같은 게임식 UI 아님
- 매수 시점의 실제 가격이 기록됨
- 과거 시세를 분 단위로 합성해 재생하는 방식은 **기각** — "사실을 지어내지 않는다"는 원칙과 충돌

## 핵심 기능 3 — 공시 학습

- DART Open API로 신규 공시 수신
- 공시 유형별 용어 사전 + 쉬운 요약으로 변환
- 이벤트 카드로 노출 (공시 발생 → 카드 등장 → 클릭 시 쉬운 설명)
- 역할: **근거의 재료 공급** + 리뷰 계산층의 공시 매칭 입력

## 화면

| # | 화면 | 내용 |
| --- | --- | --- |
| 1 | 홈 | 총자산, 오늘 손익, 보유 종목 리스트, 미확인 리뷰 뱃지 |
| 2 | 종목 탐색 | 종목 검색 + 오늘의 공시 카드 |
| 3 | 종목 상세 | 실시간 차트, 현재가, 매수 버튼 |
| 4 | **근거 입력 시트** | 주문 확정 전 필수 통과. 제품의 심장 |
| 5 | 보유 상세 | 평가손익 + **lot 목록**(각 lot의 근거 재노출), 매도 |
| 6 | 즉시 리뷰 | 매도 직후 카드 |
| 7 | 밑줄 기록 | 지난 거래 + 리뷰 목록, 근거 유형별 누적 성적 |

### 근거 유형별 누적 성적 (화면 7)

"실적 기대로 그은 밑줄 7개 중 3개 성공 / 테마·뉴스 4개 중 0개" 형태. 조회 시점에 계산하므로 배치·스케줄러 불필요. lot 단위 설계 덕분에 데이터가 이미 이 모양으로 쌓인다.

## 미결정

- **근거 메모 입력 방식** — 자유 서술 vs 유형 칩 선택 + 한 줄 보충. AI 리뷰 품질을 좌우하는 선결 조건. PoC 검증에서 결정
- **리뷰 생성 타이밍** — 매도 직후 동기 생성(2~5초 로딩 UX) vs 비동기 후 알림
- 게임 룰 — 가상 시드머니 규모, 레벨업 등
