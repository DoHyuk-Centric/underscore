# 밑줄 — DB 선택

## 결론

밑줄의 주 데이터베이스는 **PostgreSQL**로 정한다. ORM은 **Drizzle ORM**을 사용한다.

PostgreSQL은 가상 잔액, 매수·매도 주문, lot, 근거, 리뷰처럼 서로 연결된 기록을 정확하게 보관하고 한 번에 변경해야 하는 밑줄의 핵심 문제에 가장 잘 맞는다.

이 결정은 데이터 모델과 거래 처리의 기준이다. 실제 PostgreSQL 인스턴스의 배포 위치(EC2 동거 또는 AWS RDS)는 배포 단계에서 별도로 정한다.

## 핵심 기능이 해결해야 하는 문제

| 핵심 기능 | 해결해야 하는 문제 | PostgreSQL로 해결하는 방식 |
| --- | --- | --- |
| 가상 매수 | 잔액 차감, lot 생성, 주문 기록 중 하나만 반영되면 안 됨 | 트랜잭션으로 세 변경을 한 번에 확정한다 |
| 가상 매도 | FIFO 순서로 여러 lot을 소진하고, 매도 기록과 잔액을 함께 갱신해야 함 | lot·fill·order를 관계로 저장하고 트랜잭션으로 일괄 처리한다 |
| 동시 주문 | 같은 사용자가 여러 기기에서 주문하거나 버튼을 연속으로 눌러 잔액을 중복 사용할 수 있음 | 최종 확정 단계에서 잔액·lot 행을 잠그고 다시 검증한다 |
| 거래 리뷰 | 매수 근거와 매도 결과를 lot 단위로 연결해야 함 | `lot → thesis`, `lot → fill[]`, `lot → review` 관계를 유지한다 |
| 누적 성적 | 근거 유형별 성과, 보유 기간, 상대 수익을 조회해야 함 | 조인·집계 쿼리로 기록을 분석한다 |

## 거래 처리 원칙

매수·매도 버튼을 누른 순간 토스증권 주문 API를 호출하지 않는다. 밑줄은 모의투자 서비스이므로, 백엔드가 미리 수집한 최신 시세 캐시를 체결 가격으로 사용하고 PostgreSQL에 가상 거래를 확정한다.

```text
사용자 요청
  → 밑줄 세션 확인
  → 잔액·보유 lot과 가격 캐시 조회
  → PostgreSQL 트랜잭션에서 잔액·lot 최종 재확인
  → 주문 기록·lot·잔액을 함께 확정
  → 체결 결과 반환
```

가격 캐시는 PostgreSQL에 매 틱마다 기록하지 않는다. 초기에는 백엔드 프로세스 메모리에 최신 가격과 수신 시각을 보관하고, 토스증권 웹소켓 또는 REST 조회로 갱신한다. 거래 원장만 PostgreSQL에 영구 저장한다.

## PostgreSQL을 선택한 이유

### 1. 거래의 정합성

잔액이 줄었는데 lot이 생기지 않거나, 매도 기록은 생겼는데 잔액이 늘지 않는 상태는 허용할 수 없다. PostgreSQL 트랜잭션은 관련 변경을 모두 성공시키거나 모두 취소한다.

### 2. lot과 FIFO에 맞는 관계 모델

밑줄은 평균단가가 아니라 매수 1건을 lot 1개로 보관한다. lot마다 근거가 하나 붙고, 매도는 오래된 lot부터 소진되며, lot이 전량 소진되면 리뷰가 생성된다. 이런 1:1·1:N 관계와 FIFO 조회는 관계형 데이터베이스의 자연스러운 작업이다.

### 3. 조회와 분석의 확장성

사용자의 전체 보유, 종목별 lot, 아직 소진되지 않은 lot, 근거 유형별 성적, 리뷰 대상 거래를 하나의 일관된 질의 모델로 조회할 수 있다. 나중에 운영·분석 화면을 추가해도 별도 데이터 저장소 없이 시작할 수 있다.

### 4. TypeScript와 AWS 환경의 균형

PostgreSQL은 TypeScript 생태계와 Drizzle ORM 지원이 성숙했고 AWS에서 EC2 또는 RDS로 운영할 수 있다. 초기 서비스부터 운영 환경까지 선택지가 넓다.

## 다른 DB와의 비교

| 선택지 | 강점 | 밑줄에서의 판단 |
| --- | --- | --- |
| PostgreSQL | 트랜잭션, 관계, 조인, 집계 | 주 데이터베이스로 선택 |
| MSSQL | 기업 환경과 Microsoft/.NET 생태계, 강한 관리 도구 | 기능적으로 가능하지만 현재 TypeScript·AWS 중심 구성에서 얻는 이점이 작고 운영·라이선스 부담이 늘 수 있음 |
| MySQL | 일반적인 웹 서비스용 관계형 DB | 구현 가능하지만 PostgreSQL 대비 lot·분석·확장 요구에서 더 나은 이유가 뚜렷하지 않음 |
| MongoDB | 유연한 JSON 문서 | 잔액·주문·lot 간 정합성과 복잡한 관계 처리에 주 DB로는 덜 자연스러움 |
| Redis | 메모리 기반 캐시와 Pub/Sub | 시세 캐시·서버 간 이벤트 전달용 보조 저장소. 초기에는 필수 아님 |
| ClickHouse | 대규모 읽기·분석 집계 | 수억 건 단위의 분석 요구가 생길 때 검토. 주문 원장용은 아님 |

## Redis의 위치

초기 배포는 PostgreSQL과 백엔드 메모리 캐시만으로 시작한다.

Redis는 다음 상황에서 추가한다.

- EC2를 여러 대로 확장해 시세 캐시와 사용자 이벤트를 공유할 때
- 여러 웹소켓/SSE 서버가 동일한 가격 이벤트를 전달해야 할 때
- 공통 요청 제한이나 서버 세션 저장소가 필요할 때

Redis는 PostgreSQL을 대체하지 않는다. 거래의 영구 기록과 최종 정합성은 계속 PostgreSQL이 맡는다.

## 결정 범위

- 상태: 확정
- 주 DB: PostgreSQL
- ORM: Drizzle ORM
- 초기 캐시: 백엔드 프로세스 메모리
- Redis: 수평 확장 또는 공용 캐시 필요 시 추가 검토
- 실제 DB 배포 방식: 구현·배포 단계에서 결정
